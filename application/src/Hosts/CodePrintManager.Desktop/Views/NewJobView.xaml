<UserControl x:Class="CodePrintManager.Desktop.Views.NewJobView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:CodePrintManager.Desktop.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:NewJobViewModel}"
             Loaded="OnLoaded">
    <StackPanel MaxWidth="500" Margin="0,16">
        <TextBlock Text="New Print Job" FontSize="20" FontWeight="Bold" Margin="0,0,0,24" />

        <TextBlock Text="Product" Margin="0,0,0,4" />
        <ComboBox ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}"
                  DisplayMemberPath="Name" Margin="0,0,0,16" />

        <TextBlock Text="Printer" Margin="0,0,0,4" />
        <ComboBox ItemsSource="{Binding Printers}" SelectedItem="{Binding SelectedPrinter}"
                  DisplayMemberPath="Name" Margin="0,0,0,16" />

        <TextBlock Text="Quantity" Margin="0,0,0,4" />
        <TextBox Text="{Binding Quantity}" Margin="0,0,0,8" />
        <TextBlock Opacity="0.6" Margin="0,0,0,16">
            <Run Text="Available: " />
            <Run Text="{Binding AvailableCodes, Mode=OneWay}" />
        </TextBlock>

        <Button Content="Start Job" Command="{Binding StartJobCommand}"
                IsEnabled="{Binding IsProcessing, Converter={StaticResource InverseBoolConverter}}"
                HorizontalAlignment="Left" Padding="24,10" FontWeight="Bold" />

        <TextBlock Text="{Binding StatusMessage}" Margin="0,16,0,0"
                   Visibility="{Binding StatusMessage, Converter={StaticResource NullToVisibilityConverter}}" />
    </StackPanel>
</UserControl>
