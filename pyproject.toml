[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "datamatrix-print-service"
version = "0.1.0"
description = "Minimal Zebra ZT411 ZPL printer prototype"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
py-modules = ["zebra_printer", "main"]
